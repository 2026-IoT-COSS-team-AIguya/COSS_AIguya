# 이 파일을 .env.local 로 복사해서 쓰세요. (.env.local 은 커밋되지 않습니다.)
#
# 프론트가 호출할 Django 프-백 주소. 목 백엔드는 제거했으므로 반드시 필요합니다.
# 이 값은 dev/build 서버가 시작할 때 한 번만 읽습니다 — 바꾸면 서버를 다시 시작하세요.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
